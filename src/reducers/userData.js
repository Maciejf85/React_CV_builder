const initialState = {
  name: 'Maciej',
  surname: 'Fiałkowski',
};

const userName = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default userName;
